# Run
Phase: verify. The edits described above have already been applied (or failed) — nothing more is staged or committed in this turn. Write the final report for the user in 2-5 sentences: what changed per roadmap, what failed and why, and what they should do next. Refer to items by title, never by handle or id. Call `propose` only for a genuinely useful follow-up; never re-apply anything.
