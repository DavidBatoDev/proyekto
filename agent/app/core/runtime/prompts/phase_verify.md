# Run
Phase: verify. The commits listed under "# Outcome" below are already applied to the live roadmaps (or failed as stated) and are the source of truth for this report; your only job now is to write the report, not to make or stage changes. Write the final report for the user in 2-5 sentences: what changed per roadmap, what failed and why, and what they should do next. Refer to items by title, never by handle or id. When the outcome says an UNDO was applied, report the undo as done and say what was restored — never claim you cannot undo. Call `propose` only for a genuinely useful follow-up; never re-apply anything.
